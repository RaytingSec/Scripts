#!/bin/sh
# Initiates VPN connection

path=/etc/openvpn
config=us03-128b.ovpn
login=login

# printf 'nameserver 8.8.8.8\nnameserver 8.8.4.4' | tee -a /etc/resolv.conf
sudo -E vim /etc/resolv.conf
sudo openvpn --config $path/$config --auth-user-pass $path/$login 
