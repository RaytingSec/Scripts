#!/bin/bash
# OpenVPN script
# Connects to vpn via openvpn, includes tools for configuring DNS

CONFIG=user01.ovpn

AUTH=login
DIR=$HOME/.vpn

helptext="Utilities for connecting to a vpn using OpenVPN
Options:
    -c        Show current resolv.conf
    -d        Change DNS servers to Google's 8.8.8.8 and 8.8.4.4
    -k        Kill the vpn connection
    (blank)   connect to vpn and without setting dns
"


# DNS
set_dns () {
    # Google
    printf 'nameserver 8.8.8.8\nnameserver 8.8.4.4\n' | sudo -E tee /etc/resolv.conf > /dev/null
    # OpenDNS
    # printf 'nameserver 208.67.222.222\nnameserver 208.67.220.220\n' | sudo -E tee /etc/resolv.conf > /dev/null
}

# Connect to vpn
vpn_connect () {
    sudo pwd >> /dev/null # get sudo privilege
    firefox -private dnsleaktest.com & # Test vpn connection
    cd $DIR
    sudo openvpn --config "$CONFIG" --auth-user-pass "$AUTH" --auth-nocache
}

# Default start vpn
if [[ "$1" == "" ]]; then
    vpn_connect
# prints resolv.conf contents
elif [[ "$1" == "-c" ]]; then
    cat /etc/resolv.conf
# config dns
elif [[ "$1" == "-d" ]]; then
    set_dns
# force kill
elif [[ "$1" == "-k" ]]; then
    sudo pkill -KILL vpn
# help
elif [[ "$1" == "--help" ]]; then # help text
    echo "$helptext"
# unknown
else
    echo "option '$1' not recognized."
    echo "'--help' gives usage information."
fi

